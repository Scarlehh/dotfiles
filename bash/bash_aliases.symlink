# Look for the next word after sudo
alias sudo='sudo '

# Emacs
alias emacs='emacs -nw'
alias e='emacs'

# ls
alias ls='ls --color=auto --block-size=K'
alias l='ls'

# Clear shortcut
alias c="clear"

# Dropbox
alias dbc="dropbox-cli"

# Lazy config
alias wifi="nmtui"
alias wifi-edit="nmtui-edit"

# Lazy info
alias calander="cal -y"
alias weather="curl wttr.in/"